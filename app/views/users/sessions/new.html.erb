<div class="container navbar-spacer">
  <div class="row">
    <div class="login-form">
      <div class="well">
        <h2 class="page-header text-center">Please Sign in</h2>
          <%= form_for(resource, as: resource_name, url: session_path(resource_name), html:{:class => "form-horizontal"}) do |f| %>
            <div class="control-group">
              <div class="controls">
                <%= devise_error_messages! %>
              </div>
            </div>

            <div class="control-group">
              <% flash.each do |key, value| %>
                <div class="alert alert-<%= key %> text-center">
                  <%= value %>
                </div>
              <% end %>
            </div>

            <div class="control-group">
              <div class="controls">
                <%= link_to(user_omniauth_authorize_path(:google_oauth2), {:class => "btn btn-default text-center"}) do %>
                  <i class='fa fa-google-plus'> Sign in with google</i>
                <% end %>
              </div>
            </div>

            <div class="control-group">
              <div class="controls" style="padding-left:15%">
                OR
              </div>
            </div>
            <div class="control-group">
              <label class="control-label" for="inputEmail">Email</label>
              <div class="controls">
                <%= f.email_field :email, autofocus: true %>
              </div>
            </div>
            <div class="control-group">
              <label class="control-label" for="inputPassword">Password</label>
              <div class="controls">
                <%= f.password_field :password, autocomplete: "off" %>
              </div>
            </div>
            <div class="control-group inline">
              <div class="controls">
                <div class="actions">
                  <%= f.submit "Log in", :class => "btn btn-primary" %>
                </div>
              </div>
            </div>

            <div class="control-group">
              <div class="controls">
                <%# render "devise/shared/links" %>
                <%= link_to('Sign Up', new_user_registration_path)  %>
               
              </div>
            </div>
          <% end %>
      </div>
    </div>
  </div>
</div>